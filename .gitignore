# dependencies
node_modules/
mcp/node_modules/

# build output
dist/
*.tsbuildinfo

# macOS / iCloud
.DS_Store
.AppleDouble
.LSOverride
Icon?
._*

# editors
.vscode/
.idea/
*.swp

# wrangler local dev state (miniflare caches, local Durable Object sqlite).
# Machine-specific scratch — it is regenerated by `wrangler dev` and means
# nothing on anyone else's machine.
.wrangler/

# local env / secrets
.env
.env.*
!.env.example
*.local

# playtest + tooling artefacts
.playwright-mcp/
*.png
!docs/**/*.png
.vercel
